type Todo {
  name: String
  description: String
  id: String!
  completed: Boolean!
}

type Query {
  GetTodo(id: String): Todo
}

type Mutation {
  insertTodo(
    name: String
    description: String
    id: String
    completed: Boolean
  ): Todo
}
